{% extends 'base.html.twig' %}

{% block title %}TinyPuppy{% endblock %}

{% block body %}
    <h1>TinyPuppy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tiny_puppy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tiny_puppy.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ tiny_puppy.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tiny_puppy_index') }}">back to list</a>

    <a href="{{ path('tiny_puppy_edit', {'id': tiny_puppy.id}) }}">edit</a>

    {{ include('tiny_puppy/_delete_form.html.twig') }}
{% endblock %}
